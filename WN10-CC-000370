 <#
.SYNOPSIS
    This PowerShell script ensure compliance with DISA STIG WN10-CC-000370

.NOTES
    Author          : Tyler Droxler
    LinkedIn        : linkedin.com/in/tyler-droxler-549503198/
    GitHub          : github.com/tylerdroxler
    Website         : tylerdroxler.com
    Date Created    : 2025-12-01
    Last Modified   : 2025-12-01
    Version         : 1.0
    CVEs            : N/A
    Plugin IDs      : N/A
    STIG-ID         : WN10-CC-000370

.TESTED ON
    Date(s) Tested  : 2025-12-01
    Tested By       : Tyler Droxler
    Systems Tested  : Windows 10 Enterprise Ver. 10.0.19044
    PowerShell Ver. : 5.1

.USAGE
    Script MUST be ran as ADMINISTRATOR

#>

.CODE

# WN10-CC-000370: Disable Windows Hello convenience PIN
# Registry Path: HKLM\SOFTWARE\Policies\Microsoft\Windows\System
# Value Name: AllowDomainPINLogon
# Required Value: 0 (disabled)

$regPath = "HKLM:\SOFTWARE\Policies\Microsoft\Windows\System"
$regName = "AllowDomainPINLogon"
$desiredValue = 0

# Ensure registry path exists
if (-not (Test-Path $regPath)) {
    Write-Output "Creating registry path: $regPath"
    New-Item -Path "HKLM:\SOFTWARE\Policies\Microsoft\Windows" -Name "System" -Force | Out-Null
}

# Get current value
$currentValue = (Get-ItemProperty -Path $regPath -Name $regName -ErrorAction SilentlyContinue).$regName

if ($currentValue -ne $desiredValue) {
    Write-Output "Updating $regName to $desiredValue..."
    Set-ItemProperty -Path $regPath -Name $regName -Value $desiredValue
    Write-Output "Registry updated successfully."
} else {
    Write-Output "$regName is already set to $desiredValue. No changes needed."
}


 
